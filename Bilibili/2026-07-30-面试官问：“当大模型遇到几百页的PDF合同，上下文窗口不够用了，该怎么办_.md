---
title: "面试官问：“当大模型遇到几百页的PDF合同，上下文窗口不够用了，该怎么办?"
url: "https://www.bilibili.com/video/BV1FdNy6GEc8/?spm_id_from=333.1387.top_right_bar_window_history.content.click&vd_source=d0a50f3d250eed1f7d1546f70041c66b"
bvid: "BV1FdNy6GEc8"
cid: "39908213245"
author: "AI应用开发入门"
upload_date: "2026-07-13"
subtitle_lang: "中文"
created: "2026-07-30"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=116911794947308&bvid=BV1FdNy6GEc8&cid=39908213245&page=1&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

面试官问：“当大模型遇到几百页的PDF合同，上下文窗口不够用了，该怎么办?

## 字幕

`00:00` 面试官问
`00:00` 当大模型遇到几百页的PDF合同
`00:03` 上下文窗口不够用了
`00:05` 该怎么办
`00:05` 这个问题极能考验一个开发者
`00:08` 是否具备真正的生产经验
`00:10` 很多同学会回答分段或者摘要
`00:12` 但在真实的生产环境中
`00:13` 这远远不够
`00:14` 比如在金融合同审查这种零容错场景
`00:17` 简单的处理会导致物理割裂
`00:19` 关键信息丢失
`00:20` 讲一个真实的案例
`00:22` 去年我们给一家头部券商做并购合同审查
`00:25` 面对一份几百页的合同
`00:27` 里面全是复杂的法律条款
`00:29` 跨页表格和交叉引用
`00:31` 当时团队想走捷径
`00:32` 一开始直接用了简单粗暴的固定字符数切分
`00:35` 结果直接踩了雷
`00:37` 合同里一个关键的关联交易风险条款
`00:40` 正文在第12页
`00:41` 表格约束在第56页都得附录在最后
`00:45` 算法一切大模型根本拼不出完整逻辑审查报告
`00:49` 错漏百出
`00:49` 直接被甲方爸爸喷了回来
`00:52` 痛定思痛
`00:52` 我们重写了整套PIPEL
`00:54` 核心就是打了一套版面重构加层级数
`00:57` 检索加规则双轨的组合拳
`00:59` 第一步是版面分析与结构化重构
`01:01` 我们不再处理裸文本
`01:03` 而是先引入layout模型做版面分析
`01:06` 精准剥离多栏排版和跨页表格
`01:08` 修复断裂的元素
`01:10` 就像处理那种排版错乱
`01:12` 跨页还对应不上的复杂试题集一样
`01:14` 然后把文档按章节条款的层级构建文档数
`01:18` 这就保证了每个小块都是逻辑完整的意思单元
`01:22` 第二步是乘积数检索
`01:23` 配合父子文档按条款切
`01:25` 虽然逻辑完整
`01:26` 但经常遇到某一条附带几十页清单
`01:29` 单个语义块大到超token向量库根本存不下
`01:33` 为此我们对大块保留大纲数索引
`01:36` 同时对大块切隙存入向量库
`01:39` 检索时先命中小块
`01:41` 再向上映射
`01:41` 召回完整的复节点
`01:43` 配合reg fusion发散查询和重排
`01:46` 既解决了超大块检索死穴
`01:48` 又把完整上下文喂给了模型
`01:50` 第三步也是我必须强调的
`01:53` LLM绝对没有资格做严谨文件审查的唯一裁判
`01:57` 在金融场景模型一旦有数字幻觉
`01:59` 代价极大
`02:00` 所以我们用了双轨校验
`02:02` LLM负责提取语义和模糊风险点
`02:05` 规则引擎
`02:06` 负责把关键数字日期和业务库做硬比对
`02:09` 只有规则校验通过或触发人工复核
`02:12` 才能出最终报告
`02:13` 这套组合拳效果非常明显
`02:15` 长尾风险召回率大幅提升
`02:17` 业务可用性直接达标
`02:20` 当然这里面有两个大坑
`02:21` 一是切分力度的博弈
`02:23` 父子块切多细
`02:24` RERANK排多少条
`02:26` 算力和延时很容易爆炸
`02:28` 二是对LLM能力边界的认知
`02:31` 越严谨的场景
`02:32` 越要用模型做助手
`02:33` 规则做裁决
`02:34` 人工做兜底的防御架构
`02:37` 最后留个问题给大家思考和讨论
`02:39` 在你们的项目中
`02:40` 面对长文档抽取里
`02:42` LLM偶尔的幻觉
`02:43` 你们在系统架构层面是怎么做兜底
`02:46` 保证交付数据100%严谨可靠的