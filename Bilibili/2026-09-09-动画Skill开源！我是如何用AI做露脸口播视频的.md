---
title: "动画Skill开源！我是如何用AI做露脸口播视频的"
url: "https://www.bilibili.com/video/BV1d3tA6zE3L/"
bvid: ""
cid: "41360559112"
author: "Youni讲AI"
upload_date: "2026-08-28"
subtitle_lang: "中文"
created: "2026-09-09"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=117172814742078&bvid=&cid=41360559112&page=1&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

动画 Skill 已开源，欢迎领取体验：
https://github.com/laobaibaiya-alt/youni-motion

如果对你有帮助，欢迎点个 Star！你也可以在评论区告诉我，你在制作口播动画时最想解决什么问题。有问题欢迎提出！

## 字幕

`00:00` 你看到的这些视频是我用这一套sq做出来的
`00:03` 今天我就一比一的复盘我的视频制作过程
`00:06` 然后把这套动画系统直接开源
`00:09` 有效的小伙伴自取即可
`00:11` 点个收藏关注我们立刻开始
`00:14` 目前这套流程一共有六部
`00:16` 第一步是字幕出检
`00:17` 它会检查我一开始录制好导出的那个SRT内容
`00:21` 主要检查里面的术语
`00:22` 口误或者有重复录制的情况
`00:25` 当AI无法判断两个语句的前后顺序时
`00:28` 他会把不确定的位置告诉我
`00:31` 我在进行人工处理
`00:32` 第二步是字幕精条
`00:34` 这一步是在我人工处理掉不确定的内容之后
`00:38` 又重新导出了一份SRT
`00:40` 他会在新的SRT上进行文字调整和断句切分
`00:43` 第三步是场景切分
`00:45` 这里有一个限制
`00:46` 原则上我的一个场景不会最多超过40秒
`00:49` 除非在这一个场景里40秒还没有讲完
`00:52` 那就可以进行时间延长
`00:53` 并且要给我一个说明原因
`00:55` 第四步是动画方案
`00:57` 这一步会在每个场景上给我一个技术方案
`01:00` 说清楚每个场景里面观众要理解什么
`01:03` 有哪些对象关系是怎么变化的
`01:05` 第五步是场景生成
`01:07` 在确认动画方案之后
`01:09` 它会按照已经明确的方案实现
`01:11` 这一个场景的动画交互的细节书写
`01:13` 第六步是渲染
`01:15` 这一步就是根据上面的场景
`01:17` 一个一个进行渲染处理
`01:18` 同时它也会检查画布字幕
`01:20` 还有安全区域
`01:21` 包括透明通道
`01:22` 当然这六步不是我一次就调整出来的
`01:25` 也是在一次一次踩坑之后进行的
`01:28` 最后方案敲定
`01:29` 我觉得最关键的不是我分享了一个开源的工具
`01:32` 而是我想和你说清楚
`01:33` 我是如何考虑中间踩了哪些坑
`01:37` 以及每一步的思考
`01:38` 第一层改造控制字幕精准和时间切分
`01:42` 我最早碰到的问题
`01:43` 一个口播我经常会有说错啊
`01:45` 重说和多个版本
`01:47` 我在几次和AI的配合之后发现
`01:49` 如果既让AI在第一轮做内容校正
`01:52` 清理前后文关系
`01:53` 同时又给我做内容切分
`01:55` 它就会很容易发生任务冲突
`01:57` 把一些两个已经是重复的语句进行合并
`02:01` 造成这种你看到的这啊不对
`02:03` 而你看到的这
`02:04` 如果你没有再进行人工检查
`02:06` 从SRT上它就会把它并行为一段话
`02:09` 所以在这里我拆成了两轮
`02:11` 第一轮只做确定性的修正和三早留后
`02:15` 有充分的内容保留
`02:16` 最后的删除之前的有前后语句不清楚的
`02:20` 他会告诉我哪些字幕建议删除
`02:22` 由我在剪辑软件里面实际处理
`02:24` 在处理完第一轮真人的视频后
`02:26` 我再重新导出SRT内容
`02:29` 第二轮我会只让他做文字调整
`02:31` 断句和相邻字幕间的文字切分
`02:33` SRT里的字幕稳定后
`02:35` 下一个问题就是我怎么切场景
`02:37` 如果按照固定的描述
`02:38` 切一句解释或者一组连续的步骤
`02:41` 很容易被拦腰斩断
`02:42` 但如果是一个78分钟的内容
`02:45` 直接就交给动画生成
`02:46` 很容易出现啊
`02:48` 你的codex一直在跑
`02:50` 如果你中断了
`02:51` 它就可能会把之前内容全部丢失
`02:53` 而且这样一个指令让AI处理这么长的内容
`02:57` 会受到上下文的压缩影响
`02:59` 很容易前面处理的很好
`03:01` 但后面就开始胡乱生成
`03:03` 应付了事
`03:04` 我在试了几次后
`03:06` 基本上按照40秒作为一个上限
`03:08` 按照当前的问题是否回答完概念
`03:11` 是否讲清楚例子
`03:12` 是否得出结论作为标准
`03:14` 切完还要检查所有的字幕是否是有连续覆盖的
`03:18` 是否有漏掉重复或者挑号的
`03:21` 然后我又加了一个比较好的控制指令
`03:24` 一次只生产一到两场
`03:26` 目的也是一样的
`03:28` 一次生成全部
`03:29` 他只是刚开始会很好
`03:30` 后面越往后越差
`03:32` 第一层改造的重点不是让功能变多
`03:35` 而是把它的直接内容边界和错误影响的范围
`03:39` 控制住
`03:40` 第二层的改造
`03:41` 让动画不只是动起来
`03:43` 而是在表达关系
`03:44` 时间和场景稳定以后
`03:46` 我遇到的第二类问题就更隐蔽
`03:48` 动画也确实生成了
`03:49` 画面也确实在动
`03:51` 但它不一定是在解释我口播里面的字幕内容
`03:54` 最常见的结果是
`03:56` 他会把我每一句话变成一张卡片
`03:58` 依次淡入
`03:59` 再加一个箭头
`04:00` 这问题的根源是
`04:02` 我之前流程中一直没有加第四步的动画方案
`04:05` 我直接让他有一个代码方案
`04:07` 然后生成动画
`04:08` 我没有很懂代码的情况下
`04:10` 我就不会管它怎么弄
`04:11` 只能等动画生成
`04:13` 那如果错了
`04:14` 我就要又浪费额度
`04:16` 再重新调整生成
`04:17` 在第四部的动画方案中
`04:19` 我会写一份开始前有什么
`04:21` 哪句口播会发生变化
`04:23` 哪个对象做什么
`04:24` 完成后留下什么状态
`04:25` 又准备进入下一步
`04:27` 这样子我就可以在前一步审查有问题
`04:29` 直接进行修改
`04:30` 比如说新模块进入流程时
`04:32` 旧模块你就要做让位
`04:34` 结果附着到对象后
`04:36` 接触对象要有反馈
`04:38` 焦点切换时旧的焦点要做降级
`04:41` 这套判断让我发现
`04:42` 动画质量不是说用了多少种这种动效
`04:46` 而是对话关系能不能被观众连续的看见
`04:50` 第三层改造
`04:51` 把内部复杂度留给系统
`04:52` 在流程越来越严格之后
`04:54` 又会出现一个新的问题
`04:56` 在我做流程设定的时候
`04:57` AI啊总会给我出来一堆门儿
`05:00` 什么审查门
`05:01` 编号门
`05:02` 还要各种匹配编号
`05:04` 没有编号和明确的同意
`05:05` 就不能进入下一个环节
`05:07` 但是如果要我每每次都要记住这种场景
`05:09` 编号状态
`05:10` 字段审批
`05:11` 什么所谓的哈希值和固定口令
`05:13` 那这些工具呢虽然技术上很严谨
`05:16` 但我使用上真的太麻烦了
`05:18` 最后来我又做了一次方向上的调整
`05:20` 让用户只表达意图
`05:22` 内部的细节由skill自己管理
`05:25` 我可以说分析当前场景
`05:27` 或者说把开头改一下
`05:28` 按刚才的方案继续生成
`05:30` 或者继续下一段DIO
`05:32` 它会根据我的对话
`05:33` 进行真实文件和解析对象的一个匹配
`05:36` 内部编号文件映射质量证据依然保留
`05:39` 但不是我必须填写的通行证
`05:41` 目标为一时
`05:42` 直接执行多个同等候选啊
`05:44` 输入缺失或者内容冲突的时候
`05:47` 他在向我提问哦
`05:48` 对这里呢我还补充了一个
`05:50` 我目目前在第一个场景和最后一个场景
`05:53` 都会有的两个交付文件
`05:54` 一个是mp4的
`05:56` 一个是透明底的MOV
`05:57` 目的就是为了做这种开场或者结束的露脸效果
`06:01` 那做成这种效果
`06:02` 也是便于我后期直接在我真人上叠加
`06:06` MOV的动画
`06:06` 即可做到这AI和我的分工就比较清楚了
`06:10` AI更适合负责重复可记录
`06:12` 可验证的工作
`06:13` 整理确定性字幕问题
`06:15` 维护场次和文件关系
`06:17` 保存方案
`06:17` 按方案实现代码渲染
`06:19` 并且运行这些检查
`06:21` 我会继续负责表达的内容
`06:24` 比如说口播的取舍
`06:25` 语义是否准确
`06:26` 动画的表达和展示应该怎么做
`06:28` 画面审美到最后的黑屏剪辑越稳定
`06:31` 越可以验证的执行越适合交易系统
`06:34` 越要做内容取舍
`06:36` 语义或者审美的判断
`06:38` 就更应该保留我的参与
`06:40` 如果你已经有了口播内容
`06:42` 欢迎你来试试我的这个sq
`06:43` 另外我的这套经验也不只适用于视频制作
`06:47` 用AI处理复杂项目的时候
`06:49` 都可以问你输入什么内容
`06:51` 中间的结果我怎么检查
`06:53` 一次性的范围如果太大
`06:54` 要怎么拆分这个uni motion的GITHUB链接
`06:58` 我放在了简介或者置顶评论里
`07:00` 想要直接设的小伙伴
`07:01` 建议呢找一个最小的案例开始
`07:03` 如果本期视频对你有帮助
`07:05` 欢迎点赞收藏
`07:06` 关注我
`07:07` 带你了解更多的AI知识