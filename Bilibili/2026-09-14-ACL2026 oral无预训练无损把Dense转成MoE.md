---
title: "ACL2026 oral无预训练无损把Dense转成MoE"
url: "https://www.bilibili.com/video/BV1nij66ZEE8/"
bvid: "BV1nij66ZEE8"
cid: "39247219148"
author: "图明讲算法"
upload_date: "2026-06-19"
subtitle_lang: "中文"
created: "2026-09-14"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=116777459649290&bvid=BV1nij66ZEE8&cid=39247219148&page=1&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

香港中文大学ACL2026 oral的CMOE算法，在不需要预训练的情况下，可以几乎无损把Dense模型转成MoE（Mixture of Transformers）模型，训练成本及推理成本非常低，又是端侧部署的福音，思路非常的新颖，强推

## 字幕

`00:00` 嗯hello
`00:00` 大家好
`00:01` 我是图明呃
`00:02` 今天给大家分享呀
`00:03` 分享一篇MOE相关的算法
`00:06` 然后呃我们知道就是一些缩塔大模型
`00:10` 比方说deep sc或者说GLM系列
`00:12` 在训练的时候都会加上MOE的架构
`00:15` 但是往往来说预训练的成本非常的高
`00:18` 那么有没有办法
`00:20` 就是直接把dance模型转成MO1呢
`00:22` 就这篇算法提了一个非常呃新起的思路
`00:26` 就是在不需要预训练
`00:27` 然后基本上接近无损的情况下
`00:30` 可以把一个原有凳子的模型
`00:32` 直接转成MOE的架构
`00:34` 然后推理速度呃非常的快
`00:37` 然后我们来看一下它具体是什么样的
`00:40` 一个思思路
`00:41` 然后呃提出这篇paper的单位是那个港中文
`00:44` 以及诺亚以及南洋理工
`00:48` 然后首先看一下动呃动机
`00:50` 为什么我们需要把那个dance
`00:53` 直接转成MOE的架构
`00:54` 就是大语言模型
`00:56` 它的前馈网络FIN这些层
`00:59` 它大概占据了总模型参数的2/3
`01:02` 然后以及推理的计算量的大概1/2
`01:05` 然后呃FFN中的一些神经元
`01:08` 并不是所有的都会在推理的时候被激活
`01:11` 也就是有一些呃神经元它是冗余的
`01:15` 然后啊
`01:16` 我们可以通过MOE的混合专家的架构去把
`01:20` 不需要的神经神经元给它抛弃掉
`01:23` 就是在激活的时候呃
`01:25` 不激活这些没用的神经元
`01:27` 实现推力加速和参数的压缩
`01:30` 然后传统方法大概有这些局局限吧
`01:34` 首先往往都需要从头的去进行MOE的训练
`01:38` 然后MOE里面
`01:39` 他们可能还涉及到一些负载均衡
`01:41` 然后他的训练成本啊
`01:43` 然后都会非常的高
`01:45` 然后另外是微调式的MOE的转换
`01:48` 这个是需要大量的微调周期和额外的训练资源
`01:51` 然后另外是静态专家的一个划分
`01:54` 然后这样会无法适应不同的输入
`01:57` 然后它的效果会比较差
`01:59` 然后CMO1这个论文的话
`02:01` 它主要的核心洞察就是通过去呃
`02:05` 分析激活的神经元的重要性
`02:08` 然后核心思想就是
`02:10` 首先通过少量的一个校准样样本
`02:13` 比方说128个token
`02:15` 去统计
`02:15` 每一个FN神经元的激活的频率
`02:19` 和共激活的模式
`02:20` 然后第二个是基于统计的一个结果
`02:23` 将神经元去划分为共享专家和路由专家
`02:27` 就共享专家就代表呃神经元
`02:30` 它的重要性重要性重要性是最高的
`02:34` 然后路由专家的话就是次之的一些神经元
`02:37` 然后第第三个的话就是去解析式的构建路由器
`02:41` Router
`02:42` 因为我们往往知道root router
`02:44` 就是一些额判别器嘛
`02:46` 他要判别哪些token组哪些专家
`02:51` 然后呃这个CMO1的话
`02:53` 在仅仅只需要2K微调样本的情况下
`02:56` 可以在拉玛呃MISTRO的模型上
`02:59` 实现与全量微调MV相当的一个效果
`03:03` 然后它可以极大的去降低推理成本
`03:06` 然后我们看一下CMOE的一个核心的方法
`03:10` 然后他的重构流程的话大概是有这三个步骤
`03:14` 首先是激活的呃模式的分析
`03:17` 然后我们有大概三步输入校准数据
`03:20` 然后统计神经元的激活的呃频率
`03:23` 然后记录高频的神经元
`03:26` 这里面的神经元也就是FFN里面的那些嗯
`03:30` liner的一些输出
`03:32` 然后第二步的话就是专家的划分
`03:35` 然后对于高频的神经元
`03:37` 然后然后我们把它直接split到
`03:40` 共享专家的权重里面去
`03:42` 剩余的神经元根据呃共激活
`03:46` 去做k means的一个聚类
`03:48` 然后去形成我们激活的路由专家
`03:52` 然后第三步的话是路由器router的一个构建
`03:55` 就基于神经元和专家的分配
`03:58` 去重新解析计算RT的一个权重
`04:01` 这里面可能会用到一些LLM或者LM
`04:04` 相关的一些计算
`04:06` 然后根也是根据呃前面的神经元的重要性
`04:10` 得到的这些额参数
`04:13` 然后赋值给root的linear的权重
`04:15` 然后总体来说是不需要训练的
`04:18` 如果说加上一些微调的话
`04:20` 它的效果会更好一些
`04:22` 然后我们再具体来看一下
`04:24` 就是他如何去分析神经元激活的重要性能
`04:27` 首先给定一个FIN层
`04:29` 它的如输入如果是呃是X的话
`04:32` 它的输出额是Y等于这个西格玛
`04:35` 然后XW1乘以W2
`04:38` 然后W1W2也就是一些LINUR
`04:41` 然后H的话是FN的隐藏层的维度
`04:45` 然后也就是神经元的总的数量
`04:49` 然后对于校准数据集中的每一个token
`04:52` cm o e去记录每一个神经元的激活值
`04:55` 也就是经过了激活函数后的一个值
`04:58` 通过统计可以得到呃激活的一个频率
`05:02` 每个神经元在多少token上被激活
`05:05` 然后另外就是共激和额共激活的这个模式
`05:09` 就是哪些神经元倾向于同时被激活
`05:13` 然后第二个的话就是专家的划分
`05:16` 就是因为MOE里面往往会有一些
`05:19` 共享专家和路由专家
`05:21` 共享专家
`05:22` 也就是呃网络里面最重要的这一这一部分
`05:25` 就是他一定会被激活
`05:26` 路由专家的话
`05:27` 就是呃就是没有那么重要的一些神经元
`05:31` 然后基于激活频率的话
`05:33` CM1将神经元分为两类
`05:36` 首先是呃这个共享专家选取呃
`05:39` 激活频率最高的若干神经元
`05:41` 这个若干的神经元
`05:43` 它的参数是由你的共享专家的数量来确定的
`05:48` 你的共享专家数量越少
`05:50` 你激活的这个呃神经元选取的数量就越少
`05:54` 然后这些神经元在大多数输入上都会被激活
`05:58` 因此被视为通用的知识
`06:00` 对所有token生效
`06:01` 然后路由专家的话
`06:03` 也就是剩余的神经元
`06:05` 根据共激活的模式去进行k means的聚类
`06:09` 形成多个专家组
`06:10` 然后每一个token通过路由器
`06:13` 路由器去选择top k的路由专家
`06:16` 然后这个top k的这个K也是由
`06:19` 可以由用户来指定的
`06:22` 然后比方说S2A2116
`06:24` 就表示有两个共共享专家
`06:27` 然后每个token激活两个路由专家
`06:29` 然后总共的专家是16个
`06:32` 然后也就是网络在推理的时候
`06:34` 会有2+24个专家
`06:36` 然后吸收率的话相当于4÷16
`06:39` 额就是相当于只激活了25%
`06:42` 吸收率的话就是75%
`06:45` 然后第三步的话就是呃路由器的构建
`06:48` 就是传统的MOE的路由器呃
`06:50` 需要通过梯度下降来训练
`06:52` 因为它实际上就是一个linear的操作
`06:55` 然后CMO1则提出了一种解析式的构建方法
`06:58` 就是每一个路由专家的权重W1W2
`07:01` 由该专家类所有神经元的原始权重拼接而成
`07:06` 然后路由器的输出G就是被解析为这个GI
`07:11` GX呃
`07:12` 等于so max
`07:13` 然后其中这个w RT
`07:16` 就是通过分析神经元激活的统计量
`07:19` 直接计算所得出
`07:21` 不需要任何的训练
`07:22` 然后具体的这个W
`07:24` 它的每一列就是对应一个专家
`07:27` 然后他的权重与专家类神经元的平均的
`07:32` 激光强度是呈成比例的
`07:34` 从而使得路由器能够反映真实的输入
`07:37` 专家的一个亲和力
`07:40` 然后第四节的话就是可选微调
`07:43` 就是虽然moo1可以实现完全无需训练的工作
`07:48` 但是发现就是作者发现使用少量的一些样本
`07:51` 比如2K进行轻量的LAURA的微调
`07:54` 可以进一步的去提升呃性能
`07:56` 然后微调的时候只需要更新这个router的参数
`08:01` 和专家的偏执项
`08:03` 也就是说他训练只有LINUR的参数
`08:05` 还有BIOS
`08:06` 然后冻结所有专家的权重
`08:09` 就是这些专家的权重是从dance模型之间copy过来的
`08:13` 然后不需要任何的训练
`08:14` 所以说训练的成本非常低
`08:17` 然后第三个的话我们看一下实验的效果
`08:20` 然后这个CMA在呃拉马miss cho
`08:24` 多种配置下进行了全面的评估
`08:26` 首先是看一下这个ping控和度
`08:29` 就是拉玛7B
`08:31` 然后用呃SRA26的这个配置
`08:34` 然后PPL大概是5.68
`08:37` 然后相比于原始模型
`08:39` 甚至还额提高了0.21
`08:43` 然后呃就相就相当于增加了0.21
`08:48` 几乎是无损的
`08:49` 还是越低越好嘛
`08:51` 然后基本上差不多
`08:53` 跟原始的这个稠密的凳子模型
`08:55` 它是原始的5.47
`08:56` 然后mixture7B的话也是在这个配置下
`09:00` 然后困惑度是4.92
`09:02` 增加了0.18
`09:03` 也相当于呃无损的一个状态
`09:07` 然后第二个就是零零样本的下游任务
`09:10` 就是在多个零样本推理基准上
`09:12` 然后M1也展现了非常出色的能力
`09:15` 在拉玛7B呃
`09:17` 的平均准确率
`09:19` 仅比原始的凳子模型下降了0.8%
`09:22` metro7B上
`09:24` 然后性能的保值率超过了98.5
`09:26` 然后CMO1能够在非常小的代价不需要训练
`09:31` 或者说仅需2K样本微调的情况下
`09:34` 可以把稠密的FIN重构为奇数MO1
`09:38` 同时几乎完全保持性能
`09:41` 然后第四部分
`09:42` 我们看一下cm1的核心的代码的实现
`09:46` 首先是额如何去分析神经元的激活的频率啊
`09:52` 首先是通过这个这个函数来实现的
`09:56` 然后里面大概会有这样一些操作吧
`09:59` 首先呃它的输入就是你的模型
`10:02` 以及你的激活的token
`10:04` 我们可以默认用128个
`10:06` 然后这个layer status
`10:08` 就是存储每一个FIN的神经元的激活的次数
`10:12` 然后这个地方会注册一个hook
`10:16` 去捕获FM的激活的输出
`10:20` 然后呃会存到这个layer status status里面去
`10:25` 然后然后为所有的FN层
`10:29` 去注册前面的这个hook
`10:31` 然后在校准的数据上去进行一个前向传播
`10:37` 然后最后呃运行完了之后
`10:39` 把前面的hook给他移除移除掉
`10:41` 然后再返回每一个FM的神经元的激活的计数好
`10:46` 第二部分就是专家的划分
`10:48` 就是如何划分共享专家和激活专家
`10:53` 首先呃step1的话就是根据激活的频率去排序
`10:58` 对神经元做一个排序
`11:00` 然后第二个就是选出高频的神经元
`11:02` 作为共享的专家
`11:04` 每一个共享专家它包含相同数量的一个神经元
`11:07` 数量的话就是呃hidden除以呃
`11:11` 除以这个共享专家和金融专家的一个数量
`11:14` 因为神经元的总数就是这个黑的
`11:18` 然后每一个专家他需要的神经元
`11:21` 就是这个H除以总和专家的数量
`11:26` 然后这个共享专家就是一个通过这样的一个list
`11:30` 然后把它存到这个list里面去
`11:33` 然后就是存的就是每一个专家的
`11:37` 不就每一个神经元
`11:38` 他的index
`11:39` 就是最后会根据这个index去从原始的dance模型里面
`11:44` 去去取它相应的LINUR的一些权重
`11:48` 还有第三步的话就是剩余神经元用于路由专家
`11:54` 这个也就是这个routine experts
`11:57` 也是把相应的额他的index给他存下来
`12:01` 然后第三步的话就是去构建前面的
`12:04` router的路由器
`12:07` 然后首先第一步就是嗯嗯路由器的话
`12:11` 它基本上就是一个NANA的这样的算子嘛
`12:14` 然后啊我们会提取属于该专家的神经元
`12:18` 对应的W1列和W2列对
`12:21` 然后把它复制到这两个list里面去
`12:24` 然后第二个步的话去构建就是路由器的权重
`12:28` 这个w RT对于每一个专家一
`12:31` 他的路由权重
`12:32` 就等于专家类神经元的平平均的激活强度
`12:36` 再乘以W1的这样的一个平均值
`12:40` 对啊
`12:41` 这个地方是取得touch me就是取的平均值
`12:45` 然后最后呃输出
`12:47` 最后我们的router的相应的一个weight
`12:50` 然后第四步的话就是CMOE的一个呃
`12:54` 他网络的一个全向传播的逻辑
`12:56` 就是呃里面首先看一下这里面的共享专家
`13:00` 他是直接从原始的DTH权重中去取它
`13:04` 相应的index代表的权重
`13:08` 然后路由专家的话也是同样的
`13:11` 从原始的权重里面去不额呃
`13:16` 激活的专家的话
`13:17` 也是从原始的权重里面去取取这样的一个weight
`13:22` 然后呃
`13:23` 路由专家的话就是通过前面的一些呃
`13:27` 激活的一些重要性
`13:29` 然后计算得到它相应的权重的重要性
`13:34` 然后他在forward的时候大概有这四个步骤
`13:37` 首先是共享专家去forward
`13:40` 然后对所有token去做激活的一个输出
`13:43` 然后得到这个shell out out
`13:45` 然后另外是路由器专家去得到他的一个输出
`13:50` 也就是这个router props
`13:52` 然后第三个的话去通过top k去路由相应的专家
`13:58` 然后并对它做一个规划
`14:00` 然后第四的话
`14:02` 第四个
`14:03` 第四个的话就是呃
`14:05` 路由专家的一个加权的一个计算
`14:08` 然后最后把共享专家和路由专家啊加和
`14:11` 然后最后返回得到最终的输出
`14:15` 然后我们看一下第五的话
`14:17` 就是如何呃快速的去一键运行这个CMO1
`14:22` 就是GITHUB上有开源的代呃
`14:24` 开源代码
`14:25` 然后里面给了一些啊环境的依赖
`14:28` 对
`14:31` 然后呃它的运行的入口就是这个run CMO
`14:35` 一点PY啊
`14:36` 大家可以自己抽空试一下
`14:38` 然后里面有一些关键参数
`14:40` 就是像这个n shell就是共享专家的数量
`14:43` 然后n activated就是激活专家的数量
`14:48` 然后路由专家的数量就是这个n expert
`14:51` 然后里面效果最好的就是二二十六
`14:54` 然后这个就是微调样本的数量
`14:56` 然后以及微调的学习率
`15:01` 然后再总结一下这个CM1的一个核
`15:04` 核心的贡献
`15:05` 就是它是证明了
`15:06` 基于激活模式的f n to m o e重构
`15:10` 是一种非常高效可行的方法
`15:12` 然后思维非常的新颖
`15:15` 然后他通过解析式的方式去构建了路由器
`15:18` 避免了昂贵的一个训练过程
`15:20` 使得大模型的呃系数化部署变得更加的便捷
`15:25` 就是呃这大概就是今天的一个分享
`15:29` 然后呃请大家多多关注
`15:31` 然后一键三连
`15:32` 谢谢