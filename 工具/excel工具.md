1. 对比icd编码差异， wps宏
![[对比icd编码差异.xlsm]]
```
比较两个编码结果文件。根据你提供的图片，数据结构包含病案号(medical_record_id)、序号(order_number)、诊断名称(column3_name)和ICD编码(column3_code)。
脚本功能
这个脚本会比较两个Excel文件，找出：
1. **新增编码**：在文件2中有但文件1中没有的编码
2. **删除编码**：在文件1中有但文件2中没有的编码
3. **编码变更**：诊断相同但编码发生变化的情况
```
